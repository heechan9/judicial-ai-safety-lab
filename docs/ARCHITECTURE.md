# Architecture v1.5
1. TriGuard layer — source/status guard + composite risk.
2. Bunkering layer — baseline-vs-AI + stress/evaluation contract + replay.
3. AdversarialAI layer — attack scenario + ASR + independent rerun readiness.
4. FabGuard layer — frozen baseline + drift + prioritized human review + evidence governance.
